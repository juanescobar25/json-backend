{
  "name": "json-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": "18.x"
  }
}
